# EmbyPulse-Pro 架构重构目标

## 当前判断

EmbyPulse-Pro 已经完成了一轮入口层拆分：`app/main.py` 基本保持为薄入口，应用启动、路由挂载、中间件注册、生命周期任务等职责已迁移到 `app/bootstrap/`。

当前主要架构压力不在入口文件，而在横切基础设施能力和业务模块之间的耦合：

- 数据库访问由 `app/core/database.py` 统一承载，但该模块同时负责连接管理、双库查询路由、API 穿透、迁移、表结构创建、查询性能统计和系统通知写入。
- 数据库表结构在 `app/core/database.py` 与 `app/core/db_schemas.py` 之间存在重复事实源，后续变更容易产生漂移。
- 多个路由模块承担 HTTP 入口、业务规则、SQL、外部 API 调用、缓存和后台任务逻辑，典型文件包括 `points.py`、`media_request.py`、`users.py`、`stats.py`。
- 生命周期启动逻辑仍直接调度 bot、风控、缓存刷新、社区刷新和用户门户服务，服务初始化边界尚未稳定。

## 重构主轴

本项目的重构应先采用 **基础设施边界重构优先** 的主轴，而不是直接从业务域模块化开始。

原因：

- 当前所有主要业务域都依赖 `query_db()`、`cfg`、线程启动、外部媒体服务器访问等全局能力。
- 如果先拆业务域，现有横切耦合会被复制到更多模块，后续迁移成本更高。
- 先稳定基础设施边界，可以给后续业务域拆分提供清晰、可测试、可替换的依赖入口。

## 第一阶段目标

第一阶段不追求大规模改写业务逻辑，而是先建立数据库访问稳定边界：

- 数据库层：明确系统库、播放库、API 穿透模式、迁移和表结构定义的职责归属，并完整移除 `query_db()` 这种按 SQL 字符串自动分流的兼容门面。
- 代表模块：用 `history.py` 验证播放统计读取边界，用 `api_tokens.py` 验证系统库 CRUD 边界。
- 迁移规则：新增代码不得继续引入 `query_db()`，但第一阶段不要求全仓一次性清空旧调用。

后续业务域拆分应在这些边界稳定后推进。

## 数据库层重构约束

`query_db()` 不作为长期兼容层保留。重构完成后，业务代码不得再依赖“传入 SQL 字符串，由底层猜测目标数据库和执行模式”的访问方式。

目标边界：

- 系统库访问必须显式进入系统库数据访问模块。
- 播放统计访问必须显式进入播放数据访问模块。
- API 穿透模式必须作为播放数据源的一种实现，而不是隐藏在通用 SQL 执行器里。
- 表结构定义必须只有一个事实源。
- 迁移、健康检查、备份恢复和运行时查询必须分离，不能继续集中在一个 `database.py` 中。

这个方向意味着后续重构不是简单文件拆分，而是一次数据库访问范式迁移。

## 数据库迁移策略

`query_db()` 的移除采用分批迁移策略，而不是一次性全仓替换。

第一阶段选择少量代表模块验证新边界：

- 播放统计读模型代表：优先选择 `stats` / `history` 这类主要读取 `PlaybackActivity` 的模块，用来验证播放库访问与 API 穿透数据源。
- 系统库 CRUD 代表：优先选择 `notifications` 或 `api_tokens` 这类边界较窄的模块，用来验证系统库访问、事务和错误处理。

完成代表模块迁移并通过验证后，再分批迁移高复杂度模块：

- 用户管理：`users`
- 求片/社区：`media_request`
- 积分系统：`points`
- 消息中心：`messages`
- 管理机器人与用户机器人：`bot_service`、`user_bot_service`
- 插件：`app/plugins/*`

每批迁移必须保持现有 HTTP 路由、响应结构、数据库数据兼容和外部行为稳定。业务行为变更不得混入数据库访问范式迁移。

插件不纳入第一阶段迁移范围，但必须作为兼容约束纳入数据库新 API 设计。

约束：

- 第一阶段不批量改造 `app/plugins/*` 中的数据库访问。
- 新的数据库访问边界必须能支撑插件后续迁移，不能只服务核心 routers/services。
- 插件配置、插件状态、插件日志等系统表访问应在新 API 中有明确入口。
- 后续插件迁移时应保持插件生命周期、路由注册、Pro 授权 gating 和现有配置持久化行为稳定。

## 第一阶段完成标准

`query_db()` 的终局目标是完整移除，但第一阶段的完成标准是建立新边界并通过代表模块验证，不要求一次性全仓清空所有旧调用。

第一阶段完成时应满足：

- `app/infra/db/` 新数据库边界存在，并被真实业务模块使用。
- `schema_registry` 成为表结构定义的唯一事实源；旧重复 schema 被删除、迁移或明确标记为废弃。
- 播放统计代表模块 `app/routers/history.py` 迁出 `query_db()`。
- 系统库 CRUD 代表模块 `app/routers/api_tokens.py` 迁出 `query_db()` 和散落的直接 SQLite 连接。
- 新增代码不得继续引入 `query_db()`。
- 文档列出剩余待迁移模块和批次顺序。

这一区分很重要：**完整清理 `query_db()` 是最终目标；第一阶段验收点是新范式成立并可复制。**

首批代表模块选择：

- `app/routers/history.py`：主要围绕 `PlaybackActivity` 查询，适合作为 `playback_store` 与 `history_queries` 的样板。
- `app/routers/api_tokens.py`：系统库 CRUD 边界较窄，适合作为 `system_store` 与 `api_token_dao` 的样板。
- `app/routers/notifications.py`：系统通知表访问边界较窄，作为 `system_store` 与 `notification_dao` 的后续系统库样板。

## ADR 策略

当前阶段不创建正式 ADR。

原因：

- 当前内容主要是重构目标、阶段计划和迁移边界，实施过程中仍可能根据代码细节微调。
- 这些决策已经记录在 `架构.md` 与 Trellis 任务 PRD 中，短期可满足项目上下文传递。
- 等 `app/infra/db` 公共 API 固化，并且核心模块开始依赖新边界后，再评估是否需要为“数据库访问范式迁移”创建正式 ADR。

## 数据访问风格

数据库新边界采用 **Query Service / DAO 风格**，不优先引入完整 Repository 抽象。

原因：

- 项目中的数据库访问大量是统计查询、聚合查询、场景查询和系统表 CRUD，不完全符合以实体聚合为中心的 Repository 模型。
- 播放统计数据存在 SQLite 本地模式和 Emby API 穿透模式，访问边界更接近“数据源接口”而不是传统实体仓储。
- 过早引入 Repository 会增加命名和抽象成本，无法直接解决当前的 SQL 分流、表结构重复和迁移耦合问题。

目标模块：

- `schema_registry`：数据库表结构和增量字段定义的唯一事实源。
- `system_store`：系统库连接、事务、基础执行能力。
- `playback_store`：播放数据源接口，封装 SQLite 与 API 穿透两种实现。
- `migration_service`：系统库初始化、迁移、修复、健康检查、备份恢复。
- 场景 DAO / Query Service：例如 `stats_queries`、`history_queries`、`notification_dao`、`api_token_dao`。

业务模块不得继续依赖“任意 SQL 字符串 + 自动数据库猜测”的通用访问方式。

## 目标目录结构

新的数据库基础设施边界放在 `app/infra/db/`，不继续堆叠到 `app/core/`。

原因：

- `app/core/` 已经承载配置、安全、会话、中间件、数据库、审计、授权等多种横切能力，继续扩展会形成新的大杂烩。
- `app/infra/` 更明确表达“基础设施实现”，适合作为数据库、外部客户端、后台调度等非业务能力的归属地。
- 后续业务域模块化时，可以让业务代码依赖稳定的基础设施边界，而不是继续依赖旧 `core.database`。

目标结构：

```text
app/
  infra/
    db/
      schema_registry.py
      system_store.py
      playback_store.py
      migration_service.py
  queries/
    stats_queries.py
    history_queries.py
  dao/
    notification_dao.py
    api_token_dao.py
```

`app/core/database.py` 后续只作为待删除旧入口，不承载新增能力。

`app/queries/` 与 `app/dao/` 是第一阶段的过渡目录。当前业务域边界尚未正式建立，因此不在数据库基础设施重构阶段强行引入 `app/domains/*`。等 `users`、`media_request`、`points` 等业务域边界清晰后，再把对应 Query Service / DAO 迁移到业务域内部。

## 第二阶段目标：外部客户端适配

外部系统适配层后续也应迁入 `app/infra/`，但不并入第一阶段数据库重构。

目标结构：

```text
app/
  infra/
    clients/
      emby_client.py
      tmdb_client.py
      telegram_client.py
      wecom_client.py
      moviepilot_client.py
```

原因：

- 目前 `cfg.get("emby_host")`、`cfg.get("emby_api_key")`、`requests.get()`、`requests.post()` 等外部访问逻辑分散在 routers、services 和 plugins。
- 外部客户端与数据库访问一样属于横切基础设施能力，应逐步收敛到明确边界。
- 第一阶段同时迁移数据库和所有外部客户端会导致范围失控，因此外部客户端抽象列为第二阶段目标。

## 第三阶段目标：配置访问治理

`cfg.get()` / `cfg.set()` 的散布治理列为第三阶段目标，不并入第一阶段数据库重构。

原因：

- `cfg` 当前覆盖 UI 设置、环境变量覆盖、敏感配置、bot 配置、外部 API、插件配置和运行时开关。
- 配置访问点比 `query_db()` 更分散，贸然同时清理会把数据库范式迁移扩大成全局配置系统重写。
- 第一阶段数据库重构只允许在基础设施边界内部读取必要配置，不应继续扩大业务模块中的散乱 `cfg` 访问。

目标方向：

- `media_server_settings`
- `notification_settings`
- `risk_settings`
- `request_portal_settings`
- `bot_settings`
- `plugin_settings`
