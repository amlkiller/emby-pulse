# EmbyPulse-Pro 架构重构目标与进度

## 当前判断

EmbyPulse-Pro 已经完成了一轮入口层拆分：`app/main.py` 基本保持为薄入口，应用启动、路由挂载、中间件注册、生命周期任务等职责已迁移到 `app/bootstrap/`。

数据库基础设施边界重构也已经完成：原先集中在 `app/core/database.py` / `app/core/db_manager.py` 的实现已迁入 `app/infra/db/`，`query_db()` 兼容门面已移除，重构期间保留的 `app/core/database.py` 与 `app/core/db_manager.py` 兼容壳也已删除。

当前所处阶段：

- 第一阶段：数据库访问边界重构已完成。
- 第二阶段：外部客户端适配层尚未开始。
- 第三阶段：配置访问治理尚未开始。

当前主要架构压力已经从数据库访问门面，转移到外部客户端、配置访问、生命周期服务编排和业务域收拢：

- 多个路由模块承担 HTTP 入口、业务规则、SQL、外部 API 调用、缓存和后台任务逻辑，典型文件包括 `points.py`、`media_request.py`、`users.py`、`stats.py`。
- 外部媒体服务器、TMDB、Telegram、WeCom、MoviePilot 等客户端调用仍散落在 routers、services 和 plugins 中。
- `cfg.get()` / `cfg.set()` 仍直接分布在多个业务模块中，配置访问边界尚未治理。
- 生命周期启动逻辑仍直接调度 bot、风控、缓存刷新、社区刷新和用户门户服务，服务初始化边界尚未稳定。

## 重构主轴

本项目的重构应先采用 **基础设施边界重构优先** 的主轴，而不是直接从业务域模块化开始。

原因：

- 重构启动时，主要业务域依赖 `query_db()`、`cfg`、线程启动、外部媒体服务器访问等全局能力。
- 如果先拆业务域，现有横切耦合会被复制到更多模块，后续迁移成本更高。
- 先稳定基础设施边界，可以给后续业务域拆分提供清晰、可测试、可替换的依赖入口。

## 第一阶段：数据库访问边界（已完成）

第一阶段已完成数据库访问稳定边界建设：

- 数据库层：系统库、播放库、API 穿透模式、迁移和表结构定义已收敛到明确职责边界。
- `query_db()` 已完整移除，业务代码不再依赖按 SQL 字符串自动分流的兼容门面。
- `app/core/database.py` 与 `app/core/db_manager.py` 兼容壳已删除。
- `app/infra/db/`、`app/dao/`、`app/queries/` 已被真实业务模块使用。
- 代表模块和后续批次均已迁出旧数据库门面。

后续业务域拆分应在这些边界稳定后推进。

## 数据库层重构约束

`query_db()` 已不再作为兼容层保留。业务代码不得再依赖“传入 SQL 字符串，由底层猜测目标数据库和执行模式”的访问方式。

目标边界：

- 系统库访问必须显式进入系统库数据访问模块。
- 播放统计访问必须显式进入播放数据访问模块。
- API 穿透模式必须作为播放数据源的一种实现，而不是隐藏在通用 SQL 执行器里。
- 表结构定义必须只有一个事实源。
- 迁移、健康检查、备份恢复和运行时查询必须分离，不能再集中到旧的 `core.database` 入口中。

这个方向意味着后续重构不是简单文件拆分，而是一次数据库访问范式迁移。

## 数据库迁移策略

`query_db()` 的移除已按分批迁移策略完成，而不是一次性全仓替换。

迁移过程先选择少量代表模块验证新边界：

- 播放统计读模型代表：优先选择 `stats` / `history` 这类主要读取 `PlaybackActivity` 的模块，用来验证播放库访问与 API 穿透数据源。
- 系统库 CRUD 代表：优先选择 `notifications` 或 `api_tokens` 这类边界较窄的模块，用来验证系统库访问、事务和错误处理。

完成代表模块迁移并通过验证后，再分批迁移高复杂度模块。当前这些批次已经完成：

- 用户管理：`users`
- 求片/社区：`media_request`
- 积分系统：`points`
- 消息中心：`messages`
- 管理机器人与用户机器人：`bot_service`、`user_bot_service`
- 插件：`app/plugins/*`

每批迁移必须保持现有 HTTP 路由、响应结构、数据库数据兼容和外部行为稳定。业务行为变更不得混入数据库访问范式迁移。

插件原计划不纳入第一阶段迁移范围，但最终也已完成旧数据库门面的迁出；后续插件相关重构重点应转向业务域归属、生命周期和 Pro 授权边界。

约束：

- 新的数据库访问边界必须能支撑插件后续迁移，不能只服务核心 routers/services。
- 插件配置、插件状态、插件日志等系统表访问应在新 API 中有明确入口。
- 后续插件迁移时应保持插件生命周期、路由注册、Pro 授权 gating 和现有配置持久化行为稳定。

## 第一阶段完成状态

第一阶段已经超过最初验收点：不仅建立新边界并通过代表模块验证，也完成了全仓旧门面清理。

当前状态：

- `app/infra/db/` 新数据库边界存在，并被真实业务模块使用。
- `schema_registry` 是数据库表结构和增量字段定义的新入口。
- 播放统计代表模块 `app/routers/history.py` 已迁出 `query_db()`。
- 系统库 CRUD 代表模块 `app/routers/api_tokens.py` 已迁出 `query_db()` 和散落的直接 SQLite 连接。
- 全仓 `app` / `tests` 中已无 `query_db()` 调用。
- 全仓 `app` / `tests` 中已无 `app.core.database` / `app.core.db_manager` 导入。
- 直接 `sqlite3.connect()` 当前仅保留在 `app/infra/db/` 边界内部。
- 完整测试通过：`68 passed, 4 warnings`。

这意味着：**第一阶段已经完成，可以进入第二阶段外部客户端适配或开始业务域收拢的前置设计。**

首批代表模块选择：

- `app/routers/history.py`：主要围绕 `PlaybackActivity` 查询，适合作为 `playback_store` 与 `history_queries` 的样板。
- `app/routers/api_tokens.py`：系统库 CRUD 边界较窄，适合作为 `system_store` 与 `api_token_dao` 的样板。
- `app/routers/notifications.py`：系统通知表访问边界较窄，作为 `system_store` 与 `notification_dao` 的后续系统库样板。

## ADR 策略

当前阶段不创建正式 ADR。

原因：

- 当前内容主要是重构目标、阶段计划和迁移边界，实施过程中仍可能根据代码细节微调。
- 这些决策已经记录在 `架构.md` 与 Trellis 任务 PRD 中，短期可满足项目上下文传递。
- 等 `app/infra/db` 公共 API 固化，并且核心模块开始依赖新边界后，再评估是否需要为“数据库访问范式迁移”创建正式 ADR。

## 数据访问风格

数据库新边界采用 **Query Service / DAO 风格**，不优先引入完整 Repository 抽象。

原因：

- 项目中的数据库访问大量是统计查询、聚合查询、场景查询和系统表 CRUD，不完全符合以实体聚合为中心的 Repository 模型。
- 播放统计数据存在 SQLite 本地模式和 Emby API 穿透模式，访问边界更接近“数据源接口”而不是传统实体仓储。
- 过早引入 Repository 会增加命名和抽象成本，无法直接解决当前的 SQL 分流、表结构重复和迁移耦合问题。

目标模块：

- `schema_registry`：数据库表结构和增量字段定义的唯一事实源。
- `system_store`：系统库连接、事务、基础执行能力。
- `playback_store`：播放数据源接口，封装 SQLite 与 API 穿透两种实现。
- `migration_service`：系统库初始化、迁移、修复、健康检查、备份恢复。
- 场景 DAO / Query Service：例如 `stats_queries`、`history_queries`、`notification_dao`、`api_token_dao`。

业务模块不得继续依赖“任意 SQL 字符串 + 自动数据库猜测”的通用访问方式。

## 目标目录结构

新的数据库基础设施边界放在 `app/infra/db/`，不继续堆叠到 `app/core/`。

原因：

- `app/core/` 已经承载配置、安全、会话、中间件、数据库、审计、授权等多种横切能力，继续扩展会形成新的大杂烩。
- `app/infra/` 更明确表达“基础设施实现”，适合作为数据库、外部客户端、后台调度等非业务能力的归属地。
- 后续业务域模块化时，可以让业务代码依赖稳定的基础设施边界，而不是继续依赖旧 `core.database`。

目标结构：

```text
app/
  infra/
    db/
      schema_registry.py
      system_store.py
      playback_store.py
      migration_service.py
  queries/
    stats_queries.py
    history_queries.py
  dao/
    notification_dao.py
    api_token_dao.py
```

`app/core/database.py` 与 `app/core/db_manager.py` 已删除；数据库相关新增能力必须进入 `app/infra/db/`、`app/dao/` 或 `app/queries/`。

`app/queries/` 与 `app/dao/` 是第一阶段的过渡目录。当前业务域边界尚未正式建立，因此不在数据库基础设施重构阶段强行引入 `app/domains/*`。等 `users`、`media_request`、`points` 等业务域边界清晰后，再把对应 Query Service / DAO 迁移到业务域内部。

## 终态业务域架构

业务域模块化的终态采用 **每个业务域收拢到 `app/domains/<name>/`** 的方式，而不是长期保留 `app/routers/` + `app/services/` + `app/dao/` 分散目录。

目标结构：

```text
app/
  main.py
  bootstrap/
    middleware.py
    routes.py
    lifespan.py
    runtime.py
    user_portal.py
  domains/
    users/
      router.py
      schemas.py
      service.py
      dao.py
      queries.py
      policy.py
      events.py
    playback/
      router.py
      schemas.py
      service.py
      queries.py
    media_requests/
      router.py
      schemas.py
      service.py
      dao.py
      policy.py
    points/
    notifications/
    risk/
    reports/
    plugins/
    system/
  infra/
    db/
    clients/
    config/
    scheduler/
    event_bus/
  shared/
    errors.py
    pagination.py
    time.py
```

迁移期间可以保留 `app/routers/`、`app/services/`、`app/dao/`、`app/queries/` 作为兼容壳和过渡目录，但它们不是终态架构。每迁移一个业务域，应优先把该业务域的 HTTP 入口、请求响应模型、业务编排、DAO、场景查询和规则判断收拢到同一个 `domains/<name>/` 包内。

依赖方向：

```text
domain router -> domain service -> domain dao/queries -> infra
domain service -> infra clients
bootstrap -> domain routers/services for application wiring
plugins -> plugin context/API -> domains or infra
```

禁止反向依赖：

- `infra/` 不得依赖具体业务域或 HTTP router。
- `domains/<name>/` 不得直接打开 SQLite 连接、直接拼外部服务请求或散落读取全局配置。
- 业务域之间不得随意 import 内部 DAO/queries；跨域协作应通过公开 service 接口或事件完成。
- 新增业务代码不得继续引入 `query_db()`、直接 `sqlite3.connect()`、散落 `requests.get/post` 或无边界的 `cfg.get()/cfg.set()`。

优先迁移顺序：

1. `playback`：播放历史、播放统计、仪表盘、用户画像。
2. `notifications`：系统通知、通知规则、Telegram/WeCom 推送。
3. `risk`：并发检测、处罚、风控日志。
4. `media_requests`：求片、社区缓存、TMDB/MoviePilot 联动。
5. `users`：用户元信息、注册、邀请、续期、权限路线。
6. `points`：签到、积分商城、抽奖小游戏。
7. `plugins`：插件状态、配置、生命周期和 Pro 授权 gating。

## 第二阶段目标：外部客户端适配

外部系统适配层后续也应迁入 `app/infra/`，但不并入第一阶段数据库重构。

目标结构：

```text
app/
  infra/
    clients/
      emby_client.py
      tmdb_client.py
      telegram_client.py
      wecom_client.py
      moviepilot_client.py
```

原因：

- 目前 `cfg.get("emby_host")`、`cfg.get("emby_api_key")`、`requests.get()`、`requests.post()` 等外部访问逻辑分散在 routers、services 和 plugins。
- 外部客户端与数据库访问一样属于横切基础设施能力，应逐步收敛到明确边界。
- 第一阶段同时迁移数据库和所有外部客户端会导致范围失控，因此外部客户端抽象列为第二阶段目标。

## 第三阶段目标：配置访问治理

`cfg.get()` / `cfg.set()` 的散布治理列为第三阶段目标，不并入第一阶段数据库重构。

原因：

- `cfg` 当前覆盖 UI 设置、环境变量覆盖、敏感配置、bot 配置、外部 API、插件配置和运行时开关。
- 配置访问点比 `query_db()` 更分散，贸然同时清理会把数据库范式迁移扩大成全局配置系统重写。
- 第一阶段数据库重构只允许在基础设施边界内部读取必要配置，不应继续扩大业务模块中的散乱 `cfg` 访问。

目标方向：

- `media_server_settings`
- `notification_settings`
- `risk_settings`
- `request_portal_settings`
- `bot_settings`
- `plugin_settings`
